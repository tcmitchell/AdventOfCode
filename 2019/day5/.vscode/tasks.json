{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "go build && ./day5 input.txt",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
